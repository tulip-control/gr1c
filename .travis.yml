sudo: false

addons:
  apt:
    packages:
    - graphviz

language: c
compiler:
  - gcc
  - clang

install:
  - ./get-deps.sh
  - ./get-extra-deps.sh
  - ./build-deps.sh
script:
  - make -e all
  - VERBOSE=1 make -e check

notifications:
  email: false