The `aux` directory contains code for auxiliary routines, e.g., converting among
file types.

N.B., programs in `aux` rely heavily on routines that are defined elsewhere,
usually in source code found under the `src` directory.
